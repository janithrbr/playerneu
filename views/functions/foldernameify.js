    // Helper function to convert show name to folder name (lowercase, spaces replaced with underscores)
    function foldernameify(showName) {
      return showName.toLowerCase().replace(/\s+/g, '_');
    }